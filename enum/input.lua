--- Enum capturing keys which are identical
--- For example: LEFT corresponds to both "a" and "left arrrow key"
Input = {
    LEFT = {"a", "left"}, 
    RIGHT = {"d", "right"},
    UP = {"w", "up"},
    DOWN = {"s", "down"}
}

return Input